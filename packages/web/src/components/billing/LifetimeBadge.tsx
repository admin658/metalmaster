import React from 'react';

export const LifetimeBadge: React.FC = () => {
  return (
    <div className="inline-block bg-red-800 text-white px-2 py-1 rounded text-xs font-semibold">
      LIFETIME
    </div>
  );
};

export default LifetimeBadge;
